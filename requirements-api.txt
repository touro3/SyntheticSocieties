# Lightweight requirements for the API-only deployment (Render.com free tier).
# No torch, no transformers — keeps the image under 512 MB RAM.
# Simulation runs use CPU-only policies (random, rule_based, mock, template).
#
# NOT included (only needed for offline metrics scripts, not the web worker):
#   scipy       94 MB  — calibration/distribution metrics only
#   scikit-learn 56 MB  — never imported by any API route
#   polars      102 MB  — metrics scripts only; pandas covers API parquet needs
#
# NOTE on networkx: the Flask process itself never imports networkx. It is
# required here because simulation subprocesses use sys.executable (same venv)
# and environment/network.py imports it. If simulations are ever moved to a
# dedicated worker process with its own requirements, networkx can be removed
# from this file, saving ~1.5 MB from the API image.

pyyaml==6.0.2
numpy==1.26.4
pandas==2.2.2
pyarrow==17.0.0
duckdb==1.1.0
pydantic==2.8.2
networkx==3.3
orjson==3.11.6
openai==2.35.1
python-dotenv==1.2.2

flask==3.1.3
flask-cors==6.0.0
flask-limiter==3.8.0
gunicorn==23.0.0
