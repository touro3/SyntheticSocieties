# CI requirements — CPU-only, no GPU/CUDA dependencies.
# Shared by all jobs in .github/workflows/ci.yml to avoid duplication.
# torch and transformers are intentionally excluded; llm_backend.py handles
# their absence gracefully via a lazy import guarded by _TORCH_AVAILABLE.

pyyaml==6.0.2
numpy==1.26.4
scipy==1.11.4
pandas==2.2.2
pyarrow==17.0.0
duckdb==1.1.0
pydantic==2.8.2
networkx==3.3
scikit-learn==1.5.2
matplotlib==3.9.2
pytest==8.3.2
pytest-cov==5.0.0
pytest-timeout==2.3.1
hypothesis==6.112.1
openai==1.51.0
polars==1.8.2
orjson==3.10.7
flask==3.1.3
flask-cors==6.0.2
